# GrammarLearner 设计文档

> 面向小学语文的规则化知识抽取与问答系统
> 版本：v1.0 | 日期：2026-06-04 | 项目：https://github.com/zhanghuan168/GrammarLearner

---

## 一、系统概述

GrammarLearner 是一个基于规则的中文语义知识抽取与问答系统，专注于小学语文全覆盖场景。通过分词→词性标注→句法分析→规则引擎的四层管道，将中文句子转化为结构化知识（实体、关系），并支持知识库问答。

### 1.1 核心目标

- **小学语文全覆盖**：支持判断句、描述句、能力句、喜好句、行动句、把/被字句、描写句、比较句、并列句、疑问句、连动句、双宾语句等25种句法场景
- **零外部依赖**：不使用任何深度学习模型，纯规则引擎
- **可解释**：每条知识的抽取过程可追溯
- **可扩展**：通过配置词典和规则即可支持新场景

### 1.2 技术架构

```
中文句子
  │
  ▼
┌──────────────────┐
│  分词器 (tokenizer) │  最大正向匹配 + 词典
└──────────────────┘
  │
  ▼
┌──────────────────┐
│  词性标注器(pos)   │  基于规则的词性判断
└──────────────────┘
  │
  ▼
┌──────────────────┐
│  句法分析器(parser)│  成分句法分析（SVC/SVO/SV/SV_V/BA/BEI）
└──────────────────┘
  │
  ▼
┌──────────────────┐
│  规则引擎(rule)   │  从句法树抽取事实（实体+关系）
└──────────────────┘
  │
  ▼
┌──────────────────┐
│  事实知识库(fact_kb)│  存储实体、一对多关系、事件
└──────────────────┘
  │
  ▼
┌──────────────────┐
│  问答生成器(qa)    │  问题解析 → 知识库查询 → 答案生成
└──────────────────┘
```

---

## 二、数据设计

### 2.1 知识表示模型

采用三元组 `(主体, 关系, 客体)` 表示事实：

| 句子 | 抽取结果 |
|------|---------|
| 小明是小学生 | (小明, 是, 小学生) |
| 我会唱歌 | (我, 会唱歌, 唱歌) |
| 爸爸比妈妈高 | (爸爸, 比妈妈, 高) |
| 老师给我一本书 | (老师, 给我, 书) |

### 2.2 词典设计（known_words）

**设计思路**：中文分词结果受词典影响极大，同一个词可能因为分词粒度不同导致完全不同的语义理解。

词典按**字符数降序**排列，确保优先匹配长词（避免"小鸟"被分成"小/鸟"）。

**核心分类**：

```
known_words 池：
  ├── 四大名著人物：猪八戒, 孙悟空, 刘备, 关羽, 张飞, 宋江, 贾宝玉, 林黛玉 ...
  ├── 小学高频词：小学生, 学生, 书包, 读书, 唱歌, 足球, 超市, 公园 ...
  ├── 动宾搭配：唱歌, 读书, 买菜, 踢球, 下雨 ...
  ├── 专有名词：梁山泊, 大观园, 青龙偃月刀, 结拜兄弟, 表妹, 表姐 ...
  └── 单字合并兜底：鸟, 鱼, 勇, 敢 ...
```

**质量要求**：
- 每个词必须是无歧义的（不能是其他词的子串导致错误切分）
- 优先收录双字/三字词，单字词仅作合并兜底
- 动宾搭配（唱歌/读书）单独收录，便于后续动宾合并

### 2.3 关系命名体系

**四类关系命名规则**：

```
1. 判断关系：直接用系动词
   X是Y  →  relation = "是", object = Y

2. 动宾关系：动词+宾语名词（不合并宾语）
   X吃苹果  →  relation = "吃", object = "苹果"
   X会唱歌  →  relation = "会唱歌", object = "唱歌"  （动宾合并，relation保留动词+名词）

3. 特殊句式关系：
   X比Y Adj  →  relation = "比Y", object = Adj
   X很 Adj   →  relation = "很Adj", object = Adj
   X给Y Z    →  relation = "给Y", object = Z
   X带Y去Z   →  relation = "去Z", object = Z  +  (X, 带着, Y)

4. 反向关系（自动建立）：
   X的Y是Z  →  (X, Y, Z) + (Z, 反向Y, X)
   例：(猪八戒, 师弟, 孙悟空) + (孙悟空, 师兄, 猪八戒)
```

### 2.4 知识库存储模型

```python
FactKB {
    entities: Dict[str, Entity]     # 实体库（按名字索引）
    relations: Dict[(str,str), List[str]]  # 关系库：(主体,关系) → [客体列表]（一对多）
    events: Dict[str, Event]        # 事件库（暂未充分使用）
    texts: List[Dict]              # 原始文本索引
}
```

**一对多支持**：同一主体-关系可对应多个客体，例如：
```
(唐僧, 带着, [孙悟空, 猪八戒, 沙僧])
```
查询时返回"唐僧带着孙悟空、猪八戒、沙僧去取经"。

---

## 三、代码设计

### 3.1 分词器（parser/tokenizer.py）

**算法**：最大正向匹配（MMSEG）+ 标点分割

**核心逻辑**：
```
for each segment (split by punctuations):
    i = 0
    while i < len(text):
        matched = False
        for length from 4 down to 1:
            word = text[i:i+length]
            if word in dict_set:
                tokens.append(word)
                i += length
                matched = True
                break
        if not matched:
            tokens.append(text[i])  # 单字兜底
            i += 1
```

**词典驱动**：所有分词单元通过 `known_words` 集合控制，支持：
- 人物名整体：`孙悟空`（不在字典里会分成`孙/悟/空`）
- 动宾搭配整体：`唱歌`（不在字典里会分成`唱/歌`）
- 专有名词：`结拜兄弟`、`新书包`

### 3.2 词性标注器（parser/pos_tagger.py）

**算法**：基于规则的词性判断（无统计模型）

**核心词性集**：

| 标记 | 含义 | 判断规则 |
|------|------|---------|
| N | 名词 | 单字名/常见名/非动词单字 |
| PN | 代词 | 我/你/他/她/它/们 |
| V | 动词 | 是/有/会/能/喜欢/去/来/吃/喝/唱/读... |
| V_COP | 系动词 | 是/像 |
| ADJ | 形容词 | 好/大/小/新/旧/勇敢/好吃... |
| ADV | 副词 | 很/非常/都/也... |
| NUM | 数词 | 一/二/三/第... |
| Q | 量词 | 个/只/条/本/次/位... |
| PART | 助词 | 的/地/得/着/了/过 |
| PUNCT | 标点 | ，/。/！/？... |
| P | 介词 | 和/与/把/被/给/比... |

**动词识别策略**：维护动词黑名单 + 白名单双机制：
- 常见动词直接识别：`是、有、会、能、要、去、来`
- 名词性动词恢复：检测到"代词+疑似动词+代词+名词"模式时，将位置N的动词还原为V（处理`教/送/告诉`等被误标的动词）

### 3.3 句法分析器（parser/syntactic_parser.py）

**算法**：基于成分分析的规则句法识别

**支持的句法类型**：

| 句法类型 | 模式 | 识别特征 |
|---------|------|---------|
| SVC | 主语+系动词+表语 | 系动词=是/像 + 后面有"的"字结构或无动词 |
| SVO | 主语+谓语+宾语 | 第一个动词非系动词 + 后面有名词 |
| SV | 主语+谓语 | 只有一个名词主语 + 谓语无宾语 |
| SV_V | 主语+谓语1+谓语2 | 两个连续动词（如"会飞"、"带...去"） |
| BA | 把字句 | 句中有"把"字 |
| BEI | 被字句 | 句中有"被"字 |

**关键设计**：句法分析只做粗分类，具体语义由规则引擎处理。

### 3.4 规则引擎（kb/rule_engine.py）

**核心思想**：每种句法类型对应一个 `_extract_{type}` 方法，从该句法树中抽取事实。

#### 3.4.1 SVC 抽取器（主系表）

```python
def _extract_svc(self, tree, pos_tags):
    # 1. 找系动词（V_COP）
    # 2. 合并主语（向前合并相邻名词）
    # 3. 判断是否有的字结构：X是Y的Z
    #    - Y = 的前部分（关系对象）
    #    - Z = 的后部分（关系名）
    #    - 输出：(X, Z, Y)
    #    - 自动建立反向关系：(Y, 反向Z, X)
    # 4. 简单判断句：X是Y → (X, 是, Y)
```

#### 3.4.2 SVO 抽取器（主谓宾）

```python
def _extract_svo(self, tree, pos_tags):
    # 1. 找主语（第一个N/PN，合并相邻名词）
    # 2. 找动词（跳过NUM/Q） → 动词恢复：介词"给"在双宾句中视为V
    # 3. 收集宾语（跳过NUM/Q）
    # 4. 双宾语句（给/送/教 X Y）：
    #    - 输出：(主语, {verb}X, Y)
    # 5. 动宾合并：
    #    - verb ∈ vn_bindings 时，relation = verb + obj_first
    #    - object 保持为 obj（即名词本身，不覆盖）
```

**关键修复记录**：早期版本 `object = verb + obj` 导致 `relation=object`，过滤后被判定为失败。现改为 `relation=verb+obj, object=obj`，区分关系和客体。

#### 3.4.3 SV 抽取器（主谓句）

SV 句法类型是句法分析器的"兜底"，也是最复杂的——因为它需要处理多种语义变体：

```python
def _extract_sv(self, tree, pos_tags):
    # 1. 找主语
    # 2. 找动词（跳过NUM/Q）
    #    - 特殊：介词"给"在双宾句中可作V
    #    - 特殊：某些被标注为N的动词（教/送/告诉）需恢复
    # 3. 依次尝试：
    #    a. 双宾语句（给/送/教 X Y）→ (主语, {verb}X, Y)
    #    b. 描写句（X很Adj）→ (主语, 很Adj, Adj)
    #    c. 比较句（X比Y Adj）→ (主语, 比Y, Adj)
    #    d. 动宾合并 → (主语, verb+obj, obj)
```

#### 3.4.4 SV_V 抽取器（连动/能力句）

```python
def _extract_sv_v(self, tree, pos_tags):
    # 1. 如果有两个V，第一个是能愿动词（会/能/敢）
    #    → 能力句：(主语, 会飞, 飞)
    # 2. 如果有"带/带着" → 带Y去Z结构
    #    → (主语, 带着, Y) + (主语, 去Z, Z)
```

### 3.5 事实知识库（kb/fact_kb.py）

```python
class FactKB:
    def add_relation(self, relation):
        # 一对多：key = (主体, 关系), value = [客体列表]
        if key not in self.relations:
            self.relations[key] = []
        if obj not in self.relations[key]:
            self.relations[key].append(obj)

    def query(self, question_type, entity):
        if question_type == "WHO_IS":
            rels = self.get_relations_by_subject(entity)
            if rels:
                return [f"{entity}，" + "，".join([f"{r}是{o}" for r,o in rels])]
```

### 3.6 问答生成器（qa/answer_generator.py）

**问题解析模式**（支持四大名著+小学场景）：

```python
def _parse_question(question):
    patterns = [
        (r'^([^叫把]*)是谁', 'WHO_IS', group(1)),
        (r'^(.*)的(.*)是谁', 'WHO_IS_REVERSE', group(1), group(2)),
        (r'^(.*)带着谁去(.*)', 'BRING_WHO', group(1), group(2)),
        (r'^(.*)和(.*)是什么关系', 'RELATION', group(1), group(2)),
    ]
```

---

## 四、核心算法决策记录

### 4.1 动宾合并问题（relation=object 问题）

**问题**：早期实现 `object = verb + obj_parts[0]`，导致：
```
输入：X会唱歌
输出：(X, 会唱歌, 会唱歌)  ← object == relation
```

**根因**：谓语动词被错误地当作宾语。

**解决方案**：将 `verb_rel` 和 `object` 分离：
```python
# 关系 = 动宾短语，客体 = 名词本身
verb_rel = verb + obj_parts[0]  # 会唱歌
object = obj_parts[0]            # 唱歌
relations.append((subject, verb_rel, object))
```

### 4.2 双宾语句结构（给X Y）

**问题**：POS标注器将"给"标为介词(P)，导致无法通过V识别。

**解决方案**：
```python
# 在找动词时，允许"给"作为P-WITH-V特殊情况
if pv == "V" or (wv == "给" and pv == "P"):
    verb = wv
```

宾语收集跳过NUM/Q（数量词），收集所有N/PN：
```python
obj_parts = ['我', '书']  # 从"给我一本书"中收集
# → (老师, 给我, 书)
```

### 4.3 一对多关系（师父带着多个徒弟）

**解决方案**：FactKB 的 `relations` 用 `(主体, 关系) → List[客体]` 存储，而非唯一映射。问答时遍历列表拼接答案：
```python
# 唐僧带着孙悟空、猪八戒、沙僧
(唐僧, 带着, [孙悟空, 猪八戒, 沙僧])
# → "唐僧带着孙悟空、猪八戒、沙僧去取经"
```

### 4.4 单字名词合并（沙僧/猪八戒）

**问题**：分词器把"沙僧"分成"沙/僧"，导致SVC抽取器无法正确识别主语。

**解决方案**：
```python
def _merge_noun_block(self, pos_tags, start_idx):
    # 尝试当前字+下一个字是否在known_words里
    combined = current_word + next_word
    if combined in self.known_words:
        return combined, 2  # skip 2 tokens
    return current_word, 1
```

### 4.5 词典优先策略（按长度降序）

**设计**：词典按 4→3→2→1 字排序，确保"孙悟空"优先于"悟"+"空"，避免错误切分。

---

## 五、测试覆盖矩阵

| 场景 | 句法类型 | 示例句子 | 结果 |
|------|---------|---------|------|
| 判断句 | SVC | 小明是小学生。 | ✅ |
| 判断句 | SVC | 她是我们的老师。 | ✅ |
| 描述句 | SVO | 我有一个新书包。 | ✅ |
| 描述句 | SVO | 池塘里有许多鱼。 | ✅ |
| 能力句 | SV_V | 我会唱歌。 | ✅ |
| 能力句 | SV_V | 小鸟会飞。 | ✅ |
| 喜好句 | SVO | 我喜欢读书。 | ✅ |
| 喜好句 | SVO | 他喜欢踢足球。 | ✅ |
| 行动句 | SVO | 妈妈去超市买菜。 | ✅ |
| 行动句 | SVO | 同学们去公园春游。 | ✅ |
| 把字句 | BA | 我把窗户擦干净了。 | ✅ |
| 被字句 | BEI | 小明被老师批评了。 | ✅ |
| 被字句 | BEI | 杯子被我打碎了。 | ✅ |
| 描写句 | SV | 今天天气很好。 | ✅ |
| 描写句 | SV | 他很勇敢。 | ✅ |
| 比较句 | SV | 爸爸比妈妈高。 | ✅ |
| 并列句 | SV | 我和爸爸妈妈一起吃饭。 | ✅ |
| 并列句 | SVC | 苹果和香蕉都是水果。 | ✅ |
| 疑问句 | SVO | 你喜欢读书吗？ | ✅ |
| 疑问句 | SVO | 明天会下雨吗？ | ✅ |
| 疑问词 | SVC | 谁是我们的班主任？ | ✅ |
| 疑问词 | SVC | 这是什么声音？ | ✅ |
| 连动句 | SV_V | 妈妈去市场买菜。 | ✅ |
| 双宾 | SV | 老师给我一本书。 | ✅ |
| 双宾 | SV | 妈妈送我一盒糖果。 | ✅ |

**覆盖率：25/25（100%）**

---

## 六、可改进方向

1. **复合句支持**：因为...所以...、如果...就...、虽然...但是...等复句结构尚未充分处理
2. **事件抽取**：连动句"大闹天宫"等事件名尚未从句子中独立抽取
3. **时间/地点维度**：增加了时间词和地点介词的处理能力
4. **问答生成**：目前仅支持WHO_IS等简单问题类型，WHAT/WHERE等问题待扩展

---

## 七、快速开始

```bash
# 克隆
git clone https://github.com/zhanghuan168/GrammarLearner.git
cd GrammarLearner

# 安装（无需pip，纯Python标准库）
python main.py

# 测试
python -c "
from main import GrammarLearner
m = GrammarLearner()
m.learn('孙悟空是唐僧的徒弟，会七十二变和筋斗云。')
print(m.query('孙悟空是谁'))
"
```

---

*文档版本：v1.0 | 更新：2026-06-04*